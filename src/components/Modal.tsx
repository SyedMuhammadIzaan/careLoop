import React, { memo } from 'react'
import type { ModalProps } from '../interface/ModalInterface'

const ModalComp:React.FC<ModalProps> = ({open,onOk,onCancel}) => {
  return (
    <>

    </>
  )
}

export default memo(ModalComp)